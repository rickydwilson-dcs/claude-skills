{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'CMD=$(cat | jq -r \".tool_input.command\" 2>/dev/null); if [[ \"$CMD\" =~ ^git\\ (commit|merge) ]]; then echo \"⚠️  REMINDER: Have you run /update.docs to sync documentation?\"; fi; exit 0'"
          }
        ]
      }
    ]
  }
}
